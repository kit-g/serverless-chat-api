from setuptools import setup, find_packages

setup(
    name='chat-lib',
    version='0.0.1',
    description='Dummy Python package: Models for chat app',
    author='Kit Gerasimov',
    packages=find_packages(),
)
